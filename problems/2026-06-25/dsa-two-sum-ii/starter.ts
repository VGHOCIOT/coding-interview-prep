function twoSum(numbers: number[], target: number): number[] {
  // Array is sorted ascending. Find two numbers that add to target.
  // Return 1-indexed positions. Must use O(1) extra space — no hash map.
  // TODO: implement using two pointers
  return [];
}
