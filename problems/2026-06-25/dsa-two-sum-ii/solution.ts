export function twoSum(numbers: number[], target: number): [number, number] {
  // TODO: implement using two pointers, O(n) time O(1) space
  // Return 1-indexed positions [left+1, right+1]
  return [0, 0];
}
