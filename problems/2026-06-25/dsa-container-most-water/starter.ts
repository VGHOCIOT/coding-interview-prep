function maxArea(height: number[]): number {
  // Find two lines forming the container that holds the most water.
  // Brute force is O(n²) — justify why moving the shorter pointer inward is always safe.
  // TODO: implement using two pointers
  return 0;
}
